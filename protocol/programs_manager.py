"""
Менеджер программ обучения для протокола
Хранение: № программы, Название, Номер документа, Часы
"""
import os
import json
import logging

logger = logging.getLogger(__name__)


# Базовый справочник программ
BASE_PROGRAMS = {
    "1": {"name": "Оказание первой помощи пострадавшим", "doc": "", "hours": ""},
    "2": {"name": "Использование (применение) средств индивидуальной защиты", "doc": "", "hours": ""},
    "3": {"name": "Общие вопросы охраны труда и функционирования системы управления охраной труда", "doc": "", "hours": ""},
    "4": {"name": "Безопасные методы и приемы выполнения работ при воздействии вредных и (или) опасных производственных факторов", "doc": "", "hours": ""},
    "6": {"name": "Безопасные методы и приемы выполнения земляных работ", "doc": "", "hours": ""},
    "7": {"name": "Безопасные методы и приемы выполнения ремонтных, монтажных и демонтажных работ зданий и сооружений", "doc": "", "hours": ""},
    "8": {"name": "Безопасные методы и приемы выполнения работ при размещении, монтаже, техническом обслуживании и ремонте технологического оборудования", "doc": "", "hours": ""},
    "9": {"name": "Безопасные методы и приемы выполнения работ на высоте", "doc": "", "hours": ""},
    "10": {"name": "Безопасные методы и приемы выполнения пожароопасных работ", "doc": "", "hours": ""},
    "11": {"name": "Безопасные методы и приемы выполнения работ в ограниченных и замкнутых пространствах (ОЗП)", "doc": "", "hours": ""},
    "12": {"name": "Безопасные методы и приемы выполнения строительных работ", "doc": "", "hours": ""},
    "13": {"name": "Безопасные методы и приемы выполнения работ, связанных с опасностью воздействия сильнодействующих и ядовитых веществ", "doc": "", "hours": ""},
    "14": {"name": "Безопасные методы и приемы выполнения газоопасных работ", "doc": "", "hours": ""},
    "15": {"name": "Безопасные методы и приемы выполнения огневых работ", "doc": "", "hours": ""},
    "16": {"name": "Безопасные методы и приемы выполнения работ, связанных с эксплуатацией подъемных сооружений", "doc": "", "hours": ""},
    "17": {"name": "Безопасные методы и приемы выполнения работ, связанных с эксплуатацией тепловых энергоустановок", "doc": "", "hours": ""},
    "18": {"name": "Безопасные методы и приемы выполнения работ в электроустановках", "doc": "", "hours": ""},
    "19": {"name": "Безопасные методы и приемы выполнения работ, связанных с эксплуатацией сосудов, работающих под избыточным давлением", "doc": "", "hours": ""},
    "20": {"name": "Безопасные методы и приемы обращения с животными", "doc": "", "hours": ""},
    "21": {"name": "Безопасные методы и приемы при выполнении водолазных работ", "doc": "", "hours": ""},
    "22": {"name": "Безопасные методы и приемы работ по поиску, идентификации, обезвреживанию и уничтожению взрывоопасных предметов", "doc": "", "hours": ""},
    "23": {"name": "Безопасные методы и приемы работ в непосредственной близости от полотна или проезжей части эксплуатируемых автомобильных и железных дорог", "doc": "", "hours": ""},
    "24": {"name": "Безопасные методы и приемы работ на участках с патогенным заражением почвы", "doc": "", "hours": ""},
    "25": {"name": "Безопасные методы и приемы работ по валке леса в особо опасных условиях", "doc": "", "hours": ""},
    "26": {"name": "Безопасные методы и приемы работ по перемещению тяжеловесных и крупногабаритных грузов", "doc": "", "hours": ""},
    "27": {"name": "Безопасные методы и приемы работ с радиоактивными веществами и источниками ионизирующих излучений", "doc": "", "hours": ""},
    "28": {"name": "Безопасные методы и приемы работ с ручным инструментом, в том числе с пиротехническим", "doc": "", "hours": ""},
    "29": {"name": "Безопасные методы и приемы работ в театрах", "doc": "", "hours": ""}
}


class ProgramsManager:
    """Управление данными программ обучения."""

    def __init__(self, data_dir: str):
        self.data_dir = data_dir
        os.makedirs(self.data_dir, exist_ok=True)
        self.programs_file = os.path.join(self.data_dir, "programs_data.json")
        self.programs = self._load()

    def _load(self) -> dict:
        """Загрузка данных программ."""
        if os.path.exists(self.programs_file):
            try:
                with open(self.programs_file, 'r', encoding='utf-8') as f:
                    saved = json.load(f)
                # Объединяем с базовым справочником
                merged = BASE_PROGRAMS.copy()
                for prog_id, prog_data in saved.items():
                    if prog_id in merged:
                        merged[prog_id].update(prog_data)
                return merged
            except Exception as e:
                logger.error(f"Ошибка загрузки данных программ: {e}")
        return BASE_PROGRAMS.copy()

    def save(self) -> tuple[bool, str]:
        """Сохранение данных программ."""
        try:
            with open(self.programs_file, 'w', encoding='utf-8') as f:
                json.dump(self.programs, f, ensure_ascii=False, indent=2)
            return True, "Данные программ сохранены"
        except Exception as e:
            logger.error(f"Ошибка сохранения данных программ: {e}")
            return False, f"Ошибка сохранения: {e}"

    def get_program(self, program_id: str) -> dict:
        """Получение данных программы по номеру."""
        return self.programs.get(program_id, {"name": "", "doc": "", "hours": ""})

    def get_all_programs(self) -> dict:
        """Получение всех программ."""
        return self.programs.copy()

    def update_program(self, program_id: str, doc: str = None, hours: str = None, autosave: bool = True):
        """
        Обновление номера документа и/или часов программы.
        По умолчанию автоматически сохраняет данные.
        Для пакетного обновления используйте autosave=False
        и вызывайте save() вручную после всех изменений.
        """
        if program_id not in self.programs:
            return
        if doc is not None:
            self.programs[program_id]["doc"] = doc
        if hours is not None:
            self.programs[program_id]["hours"] = hours
        if autosave:
            self.save()
